<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Black Technical Studios | The Integrated Leader</title>
    <style>
        /* CSS VARIABLES FOR THE PALETTE */
        :root {
            --bg-teal: #4A9091;
            --accent-orange: #E67E51;
            --paper-white: #F2EFE9;
            --tech-black: #1A1A1A;
            --grain: url('https://www.transparenttextures.com/patterns/stardust.png');
        }

        * { box-sizing: border-box; }

        body {
            background-color: var(--bg-teal);
            background-image: var(--grain);
            color: var(--tech-black);
            font-family: 'Courier New', Courier, monospace;
            margin: 0;
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* GRAINY OVERLAY EFFECT */
        body::before {
            content: "";
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background-image: var(--grain);
            opacity: 0.4;
            pointer-events: none;
            z-index: 9999;
        }

        /* NAVIGATION */
        nav {
            display: flex;
            justify-content: center;
            gap: 20px;
            padding: 25px;
            background: var(--paper-white);
            border-bottom: 4px solid var(--tech-black);
            position: sticky;
            top: 0;
            z-index: 100;
        }

        nav a {
            text-decoration: none;
            color: var(--tech-black);
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
            padding: 5px 10px;
            border: 2px solid transparent;
        }

        nav a:hover {
            border: 2px solid var(--accent-orange);
            background: var(--accent-orange);
            color: white;
        }

        /* HERO SECTION */
        header {
            text-align: center;
            padding: 80px 20px;
        }

        .logo-area {
            max-width: 300px;
            margin: 0 auto 20px;
            border: 5px solid var(--tech-black);
            padding: 20px;
            background: var(--paper-white);
            box-shadow: 10px 10px 0px var(--tech-black);
        }

        h1 { font-size: 2.5rem; letter-spacing: -1px; line-height: 0.9; }

        /* THE INTEGRATED LEADER JOURNEY SECTION */
        .manifesto {
            max-width: 800px;
            margin: 0 auto;
            background: var(--paper-white);
            padding: 50px;
            border: 8px solid var(--tech-black);
            box-shadow: 20px 20px 0px var(--accent-orange);
            margin-bottom: 100px;
        }

        .tagline {
            color: var(--accent-orange);
            font-weight: bold;
            text-transform: uppercase;
        }

        /* LABS GRID */
        .labs-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            max-width: 1000px;
            margin: 0 auto 100px;
            padding: 20px;
        }

        .card {
            background: var(--paper-white);
            border: 4px solid var(--tech-black);
            padding: 20px;
            transition: transform 0.2s;
        }

        .card:hover { transform: translate(-5px, -5px); box-shadow: 5px 5px 0px var(--tech-black); }

        .btn {
            display: inline-block;
            margin-top: 20px;
            padding: 15px 25px;
            background: var(--tech-black);
            color: white;
            text-decoration: none;
            font-weight: bold;
            border: none;
            cursor: pointer;
        }

        .btn:hover { background: var(--accent-orange); }

        /* SUBSTACK FORM PLACEHOLDER */
        .substack-box {
            background: #FFD54F; /* Bright yellow for attention */
            padding: 40px;
            text-align: center;
            border: 4px dashed var(--tech-black);
            max-width: 600px;
            margin: 0 auto 100px;
        }

        footer { text-align: center; padding: 50px; font-size: 0.8rem; }
    </style>
</head>
<body>

    <nav>
        <a href="#labs">Labs</a>
        <a href="#integrated">The Integrated Leader</a>
        <a href="#join">Join</a>
        <a href="#about">About</a>
    </nav>

    <header>
        <div class="logo-area">
            <h1>THE BLACK<br>TECHNICAL<br>STUDIOS</h1>
        </div>
        <p class="tagline">// HUMANITY . TECHNOLOGY . SYMBIOSIS //</p>
    </header>

    <section class="manifesto" id="integrated">
        <p class="tagline">The Origin Story</p>
        <h2>The High-Fidelity Advantage</h2>
        <p>
            My neurodivergence is not a glitch; it is my highest-resolution lens. 
            As a <strong>Queer, Neurodiverse, and Highly Sensitive (HSP)</strong> leader, 
            I see the patterns others miss and feel the friction in systems before they break.
        </p>
        <p>
            The Integrated Leader is a journey from overwhelmed to calibrated. 
            We use technical rigor to protect human sensitivity.
        </p>
        <a href="#join" class="btn">RUN SYSTEM DIAGNOSTIC // JOIN THE LIST</a>
    </section>

    <section class="labs-grid" id="labs">
        <div class="card">
            <h3>LAB 01: CORNBREAD</h3>
            <p>Researching the relationship between communal nourishment and technical infrastructure.</p>
            <small>Status: ACTIVE</small>
        </div>
        <div class="card">
            <h3>LAB 02: READING RAINBOW</h3>
            <p>Decoding digital literacy through the spectrum of neurodiverse thought.</p>
            <small>Status: ACTIVE</small>
        </div>
    </section>

    <section class="substack-box" id="join">
        <h2>JOIN THE BLACK TECHNICAL FEED</h2>
        <p>Get the latest Lab Notes and Integrated Leadership strategy in your inbox.</p>
        <form>
            <input type="email" placeholder="email@address.com" style="padding: 10px; width: 70%;">
            <button type="button" class="btn">SUBSCRIBE</button>
        </form>
    </section>

    <footer>
        &copy; 2025 THE BLACK TECHNICAL STUDIOS // BUILT FOR THE INTEGRATED LEADER
    </footer>

</body>
</html>